import React from 'react';
import style from 'src/components/spinnerImage/SpinnerImage.module.scss';

const Spinner: React.FC = () => {
  return <div className={style.spinner} />;
};

export default Spinner;
